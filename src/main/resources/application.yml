spring:
  application:
    name: notifikasi-service
  mail:
    host: smtp.gmail.com
    port: 587
    username: randuariniaulia@gmail.com 
    password: irbcpqirplsrxmvc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  rabbitmq:
    host: rabbitmq  # DIUBAH: Menggunakan nama service di docker-compose
    port: 5672
    username: admin
    password: password
    template:
      exchange: order.exchange
    listener:
      simple:
        default-requeue-rejected: false

server:
  port: 8085

app:
  rabbitmq:
    queue: order.queue
    exchange: order.exchange
    routing-key: order.routing.key
  library:
    queue: notification.queue
    exchange: notification.exchange
    routing-key: notification.key

logging:
  level:
    com.randu.rabbitmq: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true